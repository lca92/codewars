// Convert boolean values to strings 'Yes' or 'No'.

// Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

// My answer:

const boolToWord = (bool) => (bool ? `Yes` : `No`);

// My reasoning:
// 1. If an element exists, it is already true.
// 2. So if we ask: "Does bool exist?"...
// 3. ... We return 'Yes' if it does...
// 4. ... And 'No' if it doesn't.

// Kata from https://www.codewars.com/kata/53369039d7ab3ac506000467/javascript
